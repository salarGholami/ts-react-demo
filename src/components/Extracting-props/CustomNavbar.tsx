import { ComponentProps } from "react";
import Navbar from "./Navbar";

const CustomNavbar = ({}: ComponentProps<typeof Navbar>) => {
  return <div>CustomNavbar</div>;
};

export default CustomNavbar;
